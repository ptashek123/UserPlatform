apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service
  namespace: {{ .Values.namespace }}
  labels:
    app: user-platform
    component: profile-service
spec:
  replicas: {{ .Values.replicas.profileService }}
  selector:
    matchLabels:
      app: user-platform
      component: profile-service
  template:
    metadata:
      labels:
        app: user-platform
        component: profile-service
    spec:
      containers:
        - name: profile-service
          image: profile-service:{{ .Values.imageVersions.profileService }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
          env:
            - name: DB_HOST
              value: "postgres-service"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: {{ .Values.database.name | quote }}
            - name: DB_USER
              value: {{ .Values.database.user | quote }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-secret
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8002
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8002
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
---
apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: {{ .Values.namespace }}
  labels:
    app: user-platform
    component: profile-service
spec:
  type: {{ .Values.services.profileService.type }}
  ports:
    - port: {{ .Values.services.profileService.port }}
      targetPort: 8002
      protocol: TCP
  selector:
    app: user-platform
    component: profile-service
