apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: {{ .Values.namespace }}
  labels:
    app: user-platform
    component: notification-service
spec:
  replicas: {{ .Values.replicas.notificationService }}
  selector:
    matchLabels:
      app: user-platform
      component: notification-service
  template:
    metadata:
      labels:
        app: user-platform
        component: notification-service
    spec:
      containers:
        - name: notification-service
          image: notification-service:{{ .Values.imageVersions.notificationService }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
          env:
            - name: DB_HOST
              value: "postgres-service"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: {{ .Values.database.name | quote }}
            - name: DB_USER
              value: {{ .Values.database.user | quote }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-secret
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8003
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8003
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: {{ .Values.namespace }}
  labels:
    app: user-platform
    component: notification-service
spec:
  type: {{ .Values.services.notificationService.type }}
  ports:
    - port: {{ .Values.services.notificationService.port }}
      targetPort: 8003
      protocol: TCP
  selector:
    app: user-platform
    component: notification-service
